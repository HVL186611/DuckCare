<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TeacherAssessment.Simulation"
             Title="Simulation">
    <ScrollView>
        <!--
        <VerticalStackLayout Padding="30,0" Spacing="25">
            <Label 
            Text="Simulasjon"
            Style="{StaticResource Headline}"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
        </VerticalStackLayout>
        -->
        <!-- Fjern VerticalStackLayout slik at content ikke blir brukt 2 ganger-->
        <CollectionView ItemsSource="{Binding Log}">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="x:String">
                    <Label Text="{Binding .}" Padding="8"/>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <!--TODO:
        Fokuser på realtime view av reaksjoner. Query database og oppdater log.
        Implementer å gi kommentarer
        -->
    </ScrollView>
</ContentPage>